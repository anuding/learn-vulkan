//
// Created by anuding on 2019/12/30.
//
#include "Surface.h"
#include "VKContext.h"

namespace Engine::RenderCore::SurfaceHelper{

    void createSurface() {
//        if (glfwCreateWindowSurface(instance, window, nullptr, &surface) != VK_SUCCESS) {
//            throw std::runtime_error("failed to create window surface!");
//        }
    }
}
