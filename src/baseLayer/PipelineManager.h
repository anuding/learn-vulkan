#include "Pipeline.h"
#include <vector>
#include <memory>
#include <unordered_map>
class PipelineManager
{
public:
	PipelineManager();
	~PipelineManager();

private:
	//std::unordered_map<Key, std::shared_ptr<Pipeline>> pipelines;
};
